# yamllint disable rule:line-length
---
version: 2.1

description: |
  Manage installation of twdps/circleci-base-image packages on ubuntu machine executor
  (or  other debian-based non-preconfigured executor).

  Presently this includes:
  - 1password cli
  - teller (presently the default version is fixed at 1.5.6 rather than always tracking to latest)
  - Hashi Vault
  - Honeycomb buildevents
display:
  source_url: "https://github.com/ThoughtWorks-DPS/orb-base-packages"
